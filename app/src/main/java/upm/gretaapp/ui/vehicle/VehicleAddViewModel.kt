package upm.gretaapp.ui.vehicle

import androidx.compose.runtime.getValue
import androidx.compose.runtime.mutableStateOf
import androidx.compose.runtime.setValue
import androidx.lifecycle.ViewModel
import upm.gretaapp.data.VehiclesRepository

/**
 * ViewModel to validate and insert vehicles in the Room database.
 */
class VehicleAddViewModel(private val vehiclesRepository: VehiclesRepository) : ViewModel() {

    /**
     * Holds current item ui state
     */
    /*var itemUiState by mutableStateOf(VehicleUiState())
        private set*/
}

